import React,{useState, useEffect} from 'react';

import {EquippedInv} from '../../../PlayerModules';

const InventorySlider = () => {

    // const [races, setRaces] = useState([]);

    // useEffect(() => {
    //     weaponsApi().then(data => setRaces(data));
    // }, []);

    
    return (
        <EquippedInv />
    )
}

export default InventorySlider;